# Claude Code instructions

Overrides `~/.claude/CLAUDE.md` for this repo:

- **Work directly on `main`.** Commit and push to `main`; do not create feature branches or open PRs.
- Pushes to `main` trigger the GitHub Pages deploy workflow (`.github/workflows/deploy.yml`).
